export type DashboardMetrics = { total_leads: number; open_deals: number; won_deals: number; lost_deals: number; pipeline_value: string; forecast_revenue: string; pending_tasks: number; open_requests: number; leads_by_source: {source:string; count:number}[]; deals_by_stage: {stage:string; value:string; count:number}[]; tasks_by_status: {status:string; count:number}[]; requests_by_status: {status:string; count:number}[] };
export type RecordRow = { id: number; name?: string; title?: string; email?: string; status?: string; stage?: string; value?: string; updated_at?: string };
