# API

All main endpoints are versioned under `/api/v1/`. OpenAPI schema is available at `/api/schema/` and Swagger UI at `/api/docs/`.

Workflow endpoints include lead conversion, deal stage advancement, task completion, campaign sending, global search, and dashboard analytics.
